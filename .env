# -------------------------------
# GeMINI Key
# -------------------------------
GEMINI_API_KEY=AIzaSyBMROm6b6HGq2iCFt0p4IA6yTpMCybljVM

# -------------------------------
# CORE SERVER CONFIG
# -------------------------------
PORT=4000
NODE_ENV=development           # change to 'production' in prod
MONGO_URI=mongodb://root:altaf%402025@localhost:27018/?authSource=admin

# -------------------------------
# AUTH & SECURITY
# -------------------------------
JWT_SECRET=very_strong_access_token_secret
REFRESH_TOKEN_SECRET=very_strong_refresh_token_secret
ACCESS_TOKEN_EXPIRES_IN=15m         # access token lifetime
REFRESH_TOKEN_EXPIRES_IN=7d         # refresh token lifetime
BCRYPT_SALT_ROUNDS=10

# Token versioning or revocation TTL
REFRESH_COOKIE_NAME=refreshToken
REFRESH_COOKIE_MAX_AGE=604800000    # 7 days (in ms)
REFRESH_COOKIE_SAMESITE=Strict      # Strict | Lax | None
REFRESH_COOKIE_PATH=/
# REFRESH_COOKIE_DOMAIN=example.com # uncomment in production (must match your API domain)

# Password reset flow
PASSWORD_RESET_TOKEN_EXPIRES=1h     # 1 hour expiry for reset link
PASSWORD_RESET_PATH=/reset-password # frontend path appended to FRONTEND_ORIGIN
SUPPORT_URL=https://support.example.com/contact

# -------------------------------
# FRONTEND / CORS
# -------------------------------
FRONTEND_ORIGIN=http://localhost:4200

# -------------------------------
# MAIL SETTINGS
# Choose ONE method: SendGrid OR SMTP
# -------------------------------

SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=group3.jcer@gmail.com    # Gmail address
SMTP_PASS=yvcvulstxgqizyjg    #Group@20023# Gmail App Password
MAIL_FROM=group3.jcer@gmail.com
MAIL_FROM_NAME=AI AGENT DESK
MAIL_MAX_RETRIES=3
MAIL_RETRY_MIN_MS=500

# -------------------------------
# 🪵 LOGGING / MONITORING
# -------------------------------
LOG_LEVEL=info             # debug | info | warn | error
ENABLE_AUDIT_LOGS=true

# -------------------------------
# 💡 MISC
# -------------------------------
# FRONTEND_ORIGIN is used to build password reset URLs:
# e.g. ${FRONTEND_ORIGIN}/reset-password?token=...
# Add any other environment-specific configs here
DISABLE_MONGO_TRANSACTIONS=true
REFRESH_COOKIE_SAMESITE=None
NODE_ENV=development